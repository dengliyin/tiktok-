# 产品信息收集智能体配置

本目录是 `product_info_agent.py` 的自包含配置目录。这个 agent 只做本地文本整理，不调用 API。

## 文件说明

- `agent_settings.json`：默认输出目录和 Markdown 空字段策略。

## 整理方式

程序会直接读取 txt 内容，通过标题、键值对、项目符号和关键词判断，把信息归并到 OPC 产品资料字段中。适合处理“产品名：...”“核心卖点：...”这类半结构化文本，也能兜底整理自然语言段落。
